<UserControl x:Class="AdapterGenerator.UserInterface.ThemeModule.Views.ThemeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <ComboBox Grid.Column="0" VerticalContentAlignment="Center"
              Background="Transparent"
              Width="70" BorderThickness="0"
              ItemsSource="{Binding Themes}"
              DisplayMemberPath="DisplayValue"
              SelectedItem="{Binding SelectedTheme}" />
    <ComboBox Grid.Column="1" VerticalContentAlignment="Center"
              Background="Transparent"
              Width="100" BorderThickness="0"
              ItemsSource="{Binding Accents}"
              SelectedItem="{Binding SelectedAccent}" />
  </Grid>
</UserControl>